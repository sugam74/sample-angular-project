jobs:
  build:
    docker:
      - image: circleci/node:10.16.3
    environment:
      FOO: bar
    #parallelism: 3
    #resource_class: large
    working_directory: ~/sample-angular-project
    branches:
      only:
        - master
    steps:
      - run:
          name: "Npm install"
          command: npm install
          command: npm run build
